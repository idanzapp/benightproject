import { Bn8RouterOutletDirective } from './bn8-router-outlet.directive';

describe('Bn8RouterOutletDirective', () => {
  it('should create an instance', () => {
    const directive = new Bn8RouterOutletDirective();
    expect(directive).toBeTruthy();
  });
});
